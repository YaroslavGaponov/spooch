### Build a new rpm package

1. copy spooch directory to ./spooch-skel/usr/local/spooch

2. run update script

3. run build script with version as argument

 `build 0.0.9`

### Install rpm package

 `rpm -Uvh spooch.rpm`
 
 
### Remove rpm package
 
 `rpm -e spooch`
 